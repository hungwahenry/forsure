# Canonical public URL of this site (used for SEO metadata).
NEXT_PUBLIC_SITE_URL=https://forsure.fyi

# Backend API — used to fetch CMS pages (legal docs, etc.)
BACKEND_API_URL=http://localhost:3000

# Resend — waitlist sign-ups are added as contacts to a Resend audience.
# Create an Audience in the Resend dashboard and copy its ID here.
RESEND_API_KEY=
RESEND_AUDIENCE_ID=
